# Making changes in files without pushing a new hash into the git branch
```
# Make changes to files
git add .

# Amend the last commit
git commit --amend --no-edit

# Force-push the amended commit
git push --force
```
